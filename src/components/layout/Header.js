import React from 'react';

export default function Header() {
  return (
    <header
      style={{
        backgroundColor: '#333',
        color: '#fff',
        padding: '10px',
        textAlign: 'center',
      }}
    >
      <h1>Todo List</h1>
    </header>
  );
}
